import { Metadata } from 'next';

export const metadata: Metadata = {
    title: 'Публичная оферта | Авоська+',
    description: 'Пользовательское соглашение и правила сервиса Авоська+',
};

export { default } from './page';
