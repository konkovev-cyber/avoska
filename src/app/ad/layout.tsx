import { Metadata } from 'next';

export const metadata: Metadata = {
    title: 'Объявление | Авоська+',
    description: 'Подробная информация об объявлении на Авоська+',
};

export { default } from './page';
