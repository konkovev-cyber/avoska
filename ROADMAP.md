# 🚀 Avoska+ Development Roadmap

Этот документ содержит детальный технический план развития проекта.

## 👤 Phase 1: Доверие и Профили (Trust & Identity)

### 1.1 Публичный профиль продавца (`/user/[id]`)
**Задача:** Показать репутацию продавца и все его активные товары.

*   **Страница:** `src/app/user/[id]/page.tsx`
*   **Компоненты:**
    *   `UserProfileHeader`: Аватар, Имя, Рейтинг, Дата регистрации, Город.
    *   `UserActiveAds`: Сетка активных объявлений (переиспользование `AdCard`).
    *   `UserReviews`: Список отзывов о продавце.
*   **Логика:**
    1.  `GET /profiles?id=eq.[id]` — инфо о пользователе.
    2.  `GET /ads?user_id=eq.[id]&status=eq.active` — товары.
    3.  `GET /reviews?target_user_id=eq.[id]` — отзывы.

### 1.2 Расширенные отзывы
**Задача:** Дать возможность оставлять отзывы с фото и отвечать на них.

*   **Schema (Updates):**
    *   `reviews` table: добавить `images text[]`, `reply text`, `reply_date timestamptz`.
*   **UI:**
    *   Кнопка "Ответить" в профиле (только для владельца профиля).
    *   Загрузка фото при создании отзыва.

---

## 💬 Phase 2: Общение и Вовлечение (Chat & Engagement)

### 2.1 Улучшенный Чат
**Задача:** Сделать чат современным и удобным.

*   **Фото в чате:**
    *   **Storage:** Бакет `chat-images`.
    *   **Schema:** Таблица `messages` поле `type` ('text' | 'image') или `attachment_url`.
    *   **UI:** Кнопка "скрепка" -> выбор фото -> загрузка -> отображение превью.
*   **Статусы сообщений:**
    *   **Schema:** `messages` -> `is_read (boolean)`.
    *   **Logic:** При открытии чата отправлять `UPDATE messages SET is_read = true WHERE receiver_id = me`.
    *   **UI:** Две галочки (серые/синие).
*   **Индикатор печати:**
    *   Использовать `Supabase Realtime Presence`.

### 2.2 Push-уведомления
**Задача:** Возвращать пользователей в приложение.

*   **Технология:** OneSignal + Capacitor.
*   **Схема:**
    1.  При старте App запрашиваем пермишен.
    2.  Сохраняем `player_id` (OneSignal ID) в таблицу `profiles`.
*   **Триггеры (Edge Functions):**
    *   `ON INSERT messages` -> Отправить пуш получателю "Новое сообщение от [Имя]".
    *   `ON UPDATE ads (status)` -> "Ваше объявление прошло модерацию".

---

## 🔍 Phase 3: Поиск и Открытия (Discovery)

### 3.1 Продвинутые фильтры
**Задача:** Искать не только по словам, но и по параметрам.

*   **Компонент:** `SearchFilters` (Sidebar/Modal).
*   **Логика:** Генерация полей в зависимости от `category`.
    *   *Транспорт:* Год (min-max), Пробег, КПП (checkboxes).
    *   *Недвижимость:* Этаж, Площадь, Тип стен.
*   **Backend:**
    *   Использовать операторы `@>` (contains) для JSONB поля `specifications`.
    *   Пример: `supabase.from('ads').select().contains('specifications', { brand: 'BMW' })`.

### 3.2 Умная лента
**Задача:** Показывать релевантные товары.

*   **Логика:** Сохранять историю просмотров (`viewed_ads`).
*   **Алгоритм:** Искать похожие товары по категории и тегам тех товаров, которые юзер смотрел недавно.

---

## 🛡️ Phase 4: Безопасность (Safety)

### 4.1 Система жалоб
**Задача:** Очистка от спама и мошенников.

*   **Schema:** `reports`
    *   `id`, `created_at`
    *   `ad_id` (fk ads)
    *   `reporter_id` (fk profiles)
    *   `reason` (text: 'fraud', 'spam', 'rude', 'other')
    *   `comment` (text)
    *   `status` ('pending', 'resolved', 'dismissed')
*   **UI:** Кнопка "Пожаловаться" (флажок) в объявлении.

### 4.2 Админ-панель модератора
*   **Страница:** `/admin/reports`
*   **Функции:**
    *   Список жалоб.
    *   Переход к объявлению.
    *   Кнопки: "Забанить объявление", "Забанить пользователя", "Отклонить жалобу".

---

## � Phase 5: Монетизация и Продвижение (Monetization)

### 5.1 Платные услуги для объявлений
**Задача:** Дать пользователям возможность продавать быстрее.

*   **Функции:**
    *   **Turbo-продажа:** Поднятие в топ ленты раз в 24 часа.
    *   **VIP-статус:** Выделение объявления рамкой и цветом в общем списке.
    *   **Закрепление:** Удержание объявления вверху категории.
*   **UI:** Модальное окно "Продвинуть товар" с выбором пакетов.

### 5.2 Интеграция платежей
*   **Технология:** Stripe или CloudPayments (через Edge Functions).
*   **Схема:**
    1.  Юзер выбирает услугу.
    2.  Создается `payment_intent`.
    3.  После успешной оплаты Webhook дергает функцию активации услуги (обновление `ads.is_vip` или `ads.promoted_until`).

---

## 📍 Phase 6: Геолокация и Карты (Geo & Maps)

### 6.1 Поиск по карте
**Задача:** Визуально найти товары рядом.

*   **Компонент:** `AdsMap` (Leaflet или MapBox).
*   **Логика:**
    *   Кластеризация точек (Marker Clustering).
    *   Превью объявления при клике на маркер.
    *   Фильтр "В радиусе X км".
*   **Backend:** Переход на `PostGIS` в Supabase для эффективных пространственных запросов.

### 6.2 Точный выбор адреса
*   **UI:** При создании объявления — выбор точки на карте вместо простого выбора города.
*   **Reverse Geocoding:** Автоматическое определение адреса по координатам.

---

## 📱 Phase 7: Мобильный опыт (Native Experience)

**Задача:** Сблизить Web и Native части приложения.

*   **Deep Linking:** Открытие приложения по ссылке `avoska.app/ad/[id]`.
*   **Биометрия:** Вход по FaceID/TouchID через Capacitor Biometrics.
*   **Haptics:** Вибрация при лайке, отправке сообщения или ошибке.
*   **Offline Mode:** Кеширование недавно просмотренных объявлений для доступа без интернета.

---

## 💼 Phase 8: Бизнес и Аналитика (Business & Growth)

### 8.1 Аналитика для продавца
**Задача:** Показать эффективность объявлений.
*   **Метрики:** Просмотры, добавления в избранное, клики по номеру телефона, количество сообщений.
*   **UI:** Графики в личном кабинете (Recharts).

### 8.2 Бизнес-профили
*   **Статус "Компания":** Профили для магазинов с логотипом и описанием.
*   **Массовая загрузка:** Возможность загрузки товаров через XML/CSV.

---

## 🛠️ Технический долг и Оптимизация

*   [ ] **SEO:** Прописать `metadata` для всех динамических страниц (`generateMetadata`).
*   [ ] **Image Optimization:** Внедрить удаление старых картинок из Storage при удалении/редактировании объявления.
*   [ ] **Security:** Настроить Rate Limiting на отправку сообщений и создание объявлений.
*   [ ] **Testing:** Покрыть критические пути (авторизация, создание объявления, оплата) E2E тестами (Playwright).
*   [ ] **Performance:** Оптимизировать LCP за счет использования `next/image` и `priority` для главных баннеров.
*   [ ] **Types:** Уйти от `any` в ключевых компонентах ленты и чата.
