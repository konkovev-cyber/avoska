# Инструкция по развертыванию на Beget

## 📦 Что загружать на хостинг

Папка для загрузки: **`out/`**

Это готовый статический сайт, который можно разместить на любом хостинге.

## 🚀 Пошаговая инструкция для Beget

### Шаг 1: Подготовка файлов

1. Откройте папку `out/` в проекте
2. Выделите **ВСЕ** файлы и папки внутри `out/`
3. Создайте ZIP архив (или загружайте через FTP)

### Шаг 2: Загрузка на Beget

#### Вариант А: Через файловый менеджер Beget

1. Войдите в панель управления Beget
2. Перейдите в **Файловый менеджер**
3. Откройте папку вашего домена (обычно `public_html` или `ваш-домен.ru/public_html`)
4. Загрузите ZIP архив
5. Распакуйте архив
6. Убедитесь, что файл `index.html` находится в корне папки домена

#### Вариант Б: Через FTP

1. Подключитесь к Beget по FTP (данные в панели управления)
2. Перейдите в папку домена (`public_html`)
3. Загрузите все файлы из папки `out/`
4. Убедитесь, что структура сохранена

### Шаг 3: Настройка переменных окружения

⚠️ **ВАЖНО!** Статический сайт не может использовать серверные переменные окружения.

Для работы с Supabase нужно:

1. Создать файл `.env.production` в корне проекта (перед билдом)
2. Добавить туда ваши данные:
```env
NEXT_PUBLIC_SUPABASE_URL=https://tgesidmolbcqaluhphos.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=ваш-ключ
```
3. Пересобрать билд: `npm run build`
4. Загрузить обновленную папку `out/`

**ИЛИ** (проще):

Переменные уже встроены в билд из `.env.local`, поэтому текущий билд уже содержит ваши данные Supabase!

### Шаг 4: Настройка .htaccess (для корректной работы роутинга)

Создайте файл `.htaccess` в корне сайта со следующим содержимым:

```apache
<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /
  
  # Если файл или директория существует, используем их
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  
  # Для всех остальных запросов показываем index.html
  RewriteRule . /index.html [L]
</IfModule>

# Кэширование статических файлов
<IfModule mod_expires.c>
  ExpiresActive On
  ExpiresByType image/jpg "access plus 1 year"
  ExpiresByType image/jpeg "access plus 1 year"
  ExpiresByType image/gif "access plus 1 year"
  ExpiresByType image/png "access plus 1 year"
  ExpiresByType image/svg+xml "access plus 1 year"
  ExpiresByType text/css "access plus 1 month"
  ExpiresByType application/javascript "access plus 1 month"
  ExpiresByType application/x-javascript "access plus 1 month"
  ExpiresByType text/javascript "access plus 1 month"
</IfModule>

# Сжатие
<IfModule mod_deflate.c>
  AddOutputFilterByType DEFLATE text/html text/plain text/xml text/css text/javascript application/javascript application/x-javascript
</IfModule>
```

### Шаг 5: Проверка

1. Откройте ваш сайт в браузере
2. Проверьте, что:
   - Главная страница загружается
   - Категории отображаются
   - Переходы между страницами работают
   - Данные из Supabase загружаются

## 🔧 Структура файлов на хостинге

```
public_html/
├── _next/              # Статические ресурсы Next.js
├── ad/                 # Страница объявления
├── admin/              # Админ панель
├── ads/                # Список объявлений
├── categories/         # Категории
├── ... (другие страницы)
├── index.html          # Главная страница
├── favicon.ico         # Иконка сайта
├── manifest.json       # PWA манифест
└── .htaccess           # Настройки Apache (создать вручную)
```

## ⚠️ Важные моменты

1. **Переменные окружения**: Они встраиваются в билд на этапе сборки. Если нужно изменить URL Supabase - нужно пересобрать проект.

2. **CORS**: Убедитесь, что в настройках Supabase добавлен ваш домен в список разрешенных.

3. **HTTPS**: Beget автоматически предоставляет SSL сертификат. Убедитесь, что сайт работает по HTTPS.

4. **Кэширование**: Файл `.htaccess` настроит кэширование для ускорения загрузки.

## 🐛 Решение проблем

### Сайт показывает 404 на внутренних страницах

- Создайте файл `.htaccess` как описано выше

### Данные не загружаются из Supabase

- Проверьте консоль браузера (F12)
- Убедитесь, что CORS настроен в Supabase
- Проверьте, что URL и ключ правильные

### Белый экран

- Проверьте консоль браузера на ошибки
- Убедитесь, что все файлы загружены
- Проверьте права доступа к файлам (644 для файлов, 755 для папок)

## 📞 Поддержка Beget

Если возникли проблемы с хостингом:
- Техподдержка Beget: https://beget.com/ru/support
- Документация: https://beget.com/ru/kb
