# Инструкция по развертыванию сайта Avoska

## 🚀 Быстрый старт

### 1. Настройка переменных окружения

Создайте файл `.env.local` в корне проекта:

```bash
cp .env.local.example .env.local
```

Заполните его вашими данными Supabase:

```env
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
```

### 2. Установка зависимостей

```bash
npm install
```

### 3. Запуск в режиме разработки

```bash
npm run dev
```

Сайт будет доступен по адресу: http://localhost:3000

### 4. Сборка для продакшена

```bash
npm run build
```

Статические файлы будут созданы в папке `out/`

### 5. Запуск продакшен версии

```bash
npm start
```

## 📦 Развертывание

### Статический хостинг (GitHub Pages, Netlify, Vercel)

Папка `out/` содержит готовые статические файлы. Просто загрузите их на любой статический хостинг.

### Важно! ⚠️

Для работы сайта необходимо:

1. **Настроить Supabase проект** - создайте проект на https://supabase.com
2. **Выполнить SQL миграции** - используйте файлы:
   - `setup_database.sql`
   - `supabase_schema.sql`
3. **Настроить переменные окружения** - добавьте их в ваш хостинг

### Переменные окружения для хостинга

Если вы используете Vercel, Netlify или другой хостинг, добавьте эти переменные в настройках проекта:

- `NEXT_PUBLIC_SUPABASE_URL`
- `NEXT_PUBLIC_SUPABASE_ANON_KEY`

## 🔧 Структура проекта

```
avoska/
├── src/
│   ├── app/          # Страницы приложения
│   ├── components/   # React компоненты
│   └── lib/          # Утилиты и конфигурация
├── public/           # Статические файлы
├── out/              # Собранный статический сайт
└── .env.local        # Переменные окружения (не в git)
```

## 📝 Скрипты

- `npm run dev` - Запуск dev сервера
- `npm run build` - Сборка для продакшена
- `npm start` - Запуск продакшен сервера
- `npm run lint` - Проверка кода

## 🐛 Решение проблем

### Сайт не загружает данные

Проверьте:
1. Правильно ли указаны переменные окружения
2. Настроена ли база данных Supabase
3. Включен ли Row Level Security (RLS) в Supabase

### Ошибка при билде

Убедитесь, что:
1. Установлены все зависимости (`npm install`)
2. Используется Node.js версии 18 или выше
3. Нет конфликтов в package-lock.json

## 📞 Поддержка

При возникновении проблем проверьте:
- Логи консоли браузера (F12)
- Логи сервера Supabase
- Настройки CORS в Supabase
