# 🚀 Быстрая инструкция: Сборка APK в Android Studio

## Проект уже открыт в Android Studio!

### Шаг 1: Дождитесь синхронизации Gradle
- В нижней части окна вы увидите процесс "Gradle Build Running..."
- Подождите, пока он завершится (обычно 1-3 минуты)
- Если появятся ошибки - нажмите "Try Again"

### Шаг 2: Соберите APK

**Вариант A - Debug APK (для тестирования):**
1. В меню выберите: `Build → Build Bundle(s) / APK(s) → Build APK(s)`
2. Дождитесь завершения сборки
3. Появится уведомление с кнопкой "locate" - нажмите её
4. Готово! APK находится в: `app/build/outputs/apk/debug/app-debug.apk`

**Вариант B - Release APK (для публикации):**
1. В меню: `Build → Generate Signed Bundle / APK...`
2. Выберите `APK` → Next
3. Создайте новый keystore (если первый раз)
4. Следуйте инструкциям мастера

### Шаг 3: Установите APK на телефон

**Способ 1 - Через USB:**
1. Включите "Режим разработчика" на Android
2. Подключите телефон к компьютеру
3. В Android Studio: `Run → Run 'app'`

**Способ 2 - Через файл:**
1. Скопируйте `app-debug.apk` на телефон
2. Откройте файл на телефоне
3. Разрешите установку из неизвестных источников
4. Установите приложение

---

## 📍 Где найти готовый APK:

```
d:/1_sites/baraholka/android/app/build/outputs/apk/debug/app-debug.apk
```

## ⚠️ Возможные проблемы:

**"SDK not found":**
- File → Settings → Appearance & Behavior → System Settings → Android SDK
- Установите Android SDK (рекомендуется API 33 или выше)

**"Gradle sync failed":**
- File → Invalidate Caches / Restart
- Или: Build → Clean Project, затем Build → Rebuild Project

**"Java version mismatch":**
- File → Settings → Build, Execution, Deployment → Build Tools → Gradle
- Gradle JDK: выберите "Embedded JDK" или JDK 17

---

## 🔄 После изменений в коде:

```powershell
# 1. Пересоберите веб-версию
npm run build

# 2. Синхронизируйте с Android
npx cap sync android

# 3. В Android Studio: Build → Build APK(s)
```

---

## 📱 Название и иконка приложения:

**Текущее название:** Авоська+

**Изменить название:**
- Откройте: `android/app/src/main/res/values/strings.xml`
- Измените значение `<string name="app_name">`

**Изменить иконку:**
- Замените файлы в: `android/app/src/main/res/mipmap-*/`
- Или используйте: `Image Asset Studio` в Android Studio

---

Удачи! 🎉
