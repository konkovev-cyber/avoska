# Avoska+ — Проект доски объявлений

## 📋 Обзор проекта

**Avoska+** (Авоська+) — это современная веб-платформа для размещения объявлений (classifieds), построенная на **Next.js 16** с использованием **React 19**. Проект поддерживает как веб-версию, так и мобильное приложение через **Capacitor**.

### Основные возможности
- 📦 Размещение объявлений в различных категориях (Транспорт, Недвижимость, Работа, Услуги и др.)
- 👤 Профили пользователей с рейтингом и репутацией
- 💬 Система чатов между пользователями
- ❤️ Избранное для сохранённых объявлений
- 🔍 Поиск и фильтрация объявлений
- 📱 PWA + мобильное приложение (Android)
- 🗺️ Геолокация и карты (Leaflet)
- 🔔 Push-уведомления

### Технологический стек
| Категория | Технологии |
|-----------|------------|
| **Frontend** | Next.js 16, React 19, TypeScript |
| **Стили** | Tailwind CSS v4, PostCSS |
| **UI-компоненты** | Lucide React, Framer Motion, Sonner (toasts) |
| **Карты** | Leaflet, React-Leaflet |
| **Backend** | Supabase (PostgreSQL, Auth, Storage, Realtime) |
| **Мобильное приложение** | Capacitor 8, Android |
| **State Management** | Zustand |
| **Формы** | React Hook Form, Zod валидация |
| **Хостинг** | Beget (статический экспорт), FTP-деплой |

---

## 🚀 Запуск и разработка

### Установка зависимостей
```bash
npm install
```

### Переменные окружения
Создайте файл `.env.local` в корне проекта:
```env
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
```

### Режим разработки
```bash
npm run dev
```
Сайт доступен по адресу: [http://localhost:3000](http://localhost:3000)

### Сборка для продакшена
```bash
npm run build
```
Статические файлы создаются в папке `out/`

### Запуск продакшен-сервера
```bash
npm start
```

### Линтинг
```bash
npm run lint
```

---

## 📦 Деплой

### На Beget (статический хостинг)
1. Собрать проект: `npm run build`
2. Загрузить содержимое папки `out/` на хостинг
3. Создать файл `.htaccess` для SPA-роутинга

**Команды деплоя:**
```bash
# Полный деплой (сборка + FTP)
npm run deploy:full

# Только деплой без сборки
npm run deploy

# Деплой с очисткой старой версии
npm run deploy:clean
```

### Мобильное приложение (Android)
```bash
# Синхронизация с Capacitor
npx cap sync android

# Открытие в Android Studio
npx cap open android

# Сборка APK
npm run build:apk
```

См. документацию:
- [`BUILD_APK.md`](./BUILD_APK.md) — сборка APK
- [`BEGET_DEPLOYMENT.md`](./BEGET_DEPLOYMENT.md) — деплой на Beget
- [`DEPLOYMENT.md`](./DEPLOYMENT.md) — общая инструкция по развёртыванию

---

## 📁 Структура проекта

```
avoska/
├── src/
│   ├── app/              # Next.js App Router страницы
│   │   ├── ad/           # Страница объявления
│   │   ├── ads/          # Список объявлений, создание, редактирование
│   │   ├── categories/   # Категории
│   │   ├── chat/         # Чат
│   │   ├── profile/      # Профиль пользователя
│   │   ├── admin/        # Админ-панель
│   │   ├── auth/         # Авторизация
│   │   └── ...           # Другие страницы
│   ├── components/
│   │   ├── layout/       # Header, Footer, BottomNav
│   │   ├── ui/           # UI-компоненты (кнопки, модалки, карточки)
│   │   └── ...           # Другие компоненты
│   └── lib/
│       ├── supabase/     # Supabase клиент
│       ├── geo.ts        # Геолокация
│       ├── utils.ts      # Утилиты
│       └── ...           # Другие утилиты
├── public/               # Статические файлы (изображения, manifest.json)
├── out/                  # Собранный статический сайт
├── android/              # Capacitor Android проект
├── migrations/           # SQL миграции базы данных
├── scripts/              # Вспомогательные скрипты
├── _tools/               # Инструменты разработки
├── .env.local            # Переменные окружения (не в git)
├── capacitor.config.ts   # Конфигурация Capacitor
├── next.config.ts        # Конфигурация Next.js
├── tailwind.config.ts    # Конфигурация Tailwind CSS
├── tsconfig.json         # Конфигурация TypeScript
└── package.json          # Зависимости и скрипты
```

---

## 🗄️ База данных (Supabase)

### Основные таблицы
| Таблица | Описание |
|---------|----------|
| `profiles` | Профили пользователей (расширяет auth.users) |
| `categories` | Категории объявлений (иерархические) |
| `ads` | Объявления (с ENUM статусов: active, closed, archived, pending, rejected) |
| `favorites` | Избранные объявления |
| `messages` | Сообщения чата |
| `reviews` | Отзывы о пользователях |
| `reports` | Жалобы на объявления |

### SQL файлы
- [`supabase_schema.sql`](./supabase_schema.sql) — полная схема БД
- [`setup_database.sql`](./setup_database.sql) — начальная настройка
- [`migrations/`](./migrations/) — миграции

---

## 📱 Мобильное приложение

### Конфигурация Capacitor
- **App ID:** `com.avoska.plus`
- **App Name:** Авоська+
- **Web Dir:** `out/`
- **Плагины:** App, SplashScreen, Keyboard, Haptics, Push Notifications

### Скрипты
```bash
# Синхронизация веб-контента
npx cap sync android

# Открытие Android Studio
npx cap open android

# Сборка APK через PowerShell
.\build-apk.ps1
```

Готовые артефакты:
- `avoska.apk` — APK файл для установки
- `avoska.zip` — экспорт проекта

---

## 🛠️ Скрипты разработки

| Команда | Описание |
|---------|----------|
| `npm run dev` | Запуск dev-сервера |
| `npm run build` | Сборка для продакшена |
| `npm start` | Запуск продакшен-сервера |
| `npm run lint` | Проверка кода ESLint |
| `npm run deploy` | FTP деплой |
| `npm run deploy:full` | Сборка + FTP деплой |
| `npm run deploy:clean` | Очистка + сборка + деплой |
| `npm run unzip` | Распаковка на сервере |
| `npm run skills` | Список скиллов Qwen |
| `npm run skills:connect` | Подключение скилла |

### Утилиты
- `download-images.js` — загрузка изображений
- `generate_placeholders.js` — генерация плейсхолдеров
- `seed-categories.js` — сидирование категорий
- `apply-migration.js` — применение миграций
- `check_db.ts` — проверка подключения к БД

---

## 📝 Основные компоненты

### Layout
- `Header` — шапка сайта
- `BottomNav` — нижняя навигационная панель (мобильная)
- `ConditionalFooter` — условный футер
- `PageAnimatePresence` — анимации переходов

### UI-компоненты
- `AdCard` — карточка объявления
- `PromotionModal` — модальное окно продвижения
- `AppUpdateCheck` — проверка обновлений приложения
- `ResponsiveSelect` — адаптивный селект
- `HoverImageGallery` — галерея с ховером

### Карты
- `MapPicker` / `MapView` — Leaflet карты
- `YandexMapPicker` / `YandexMapView` — Яндекс.Карты

---

## 🔮 Roadmap

### Текущие приоритеты (см. [`ROADMAP.md`](./ROADMAP.md))
1. **Phase 1:** Публичные профили продавцов, расширенные отзывы
2. **Phase 2:** Улучшенный чат (фото, статусы сообщений), Push-уведомления
3. **Phase 3:** Продвинутые фильтры, умная лента рекомендаций
4. **Phase 4:** Система жалоб, админ-панель модератора
5. **Phase 5:** Платные услуги (VIP, закрепление, продвижение)
6. **Phase 6:** Поиск по карте, PostGIS
7. **Phase 7:** Deep Linking, биометрия, offline-режим
8. **Phase 8:** Аналитика для продавцов, бизнес-профили

---

## 🔧 Технические детали

### Версии
- **Node.js:** 20+
- **Next.js:** 16.1.6
- **React:** 19.2.3
- **TypeScript:** 5.x
- **Tailwind CSS:** 4.x

### Конфигурации
- **ESLint:** [`eslint.config.mjs`](./eslint.config.mjs)
- **PostCSS:** [`postcss.config.mjs`](./postcss.config.mjs)
- **TypeScript:** [`tsconfig.json`](./tsconfig.json)
- **Capacitor:** [`capacitor.config.ts`](./capacitor.config.ts)

### Алиасы импортов
```typescript
@/* → ./src/*
```

### Особенности сборки
- Статический экспорт (`output: 'export'`)
- Неоптимизированные изображения (`images.unoptimized: true`)
- Trailing slash для URL

---

## 📚 Документация

| Файл | Описание |
|------|----------|
| [`README.md`](./README.md) | Базовая информация о проекте |
| [`DEPLOYMENT.md`](./DEPLOYMENT.md) | Инструкция по развёртыванию |
| [`BEGET_DEPLOYMENT.md`](./BEGET_DEPLOYMENT.md) | Деплой на Beget |
| [`BUILD_APK.md`](./BUILD_APK.md) | Сборка Android APK |
| [`ANDROID_STUDIO_GUIDE.md`](./ANDROID_STUDIO_GUIDE.md) | Работа с Android Studio |
| [`ROADMAP.md`](./ROADMAP.md) | План развития проекта |
| [`AUDIT_REPORT.md`](./AUDIT_REPORT.md) | Отчёт аудита кода |
| [`SKILLS.md`](./SKILLS.md) | Доступные скиллы Qwen |
| [`TOOLKIT.md`](./TOOLKIT.md) | Инструменты разработки |

---

## 🐛 Решение проблем

### Данные не загружаются
- Проверьте переменные окружения `.env.local`
- Убедитесь, что CORS настроен в Supabase
- Проверьте консоль браузера (F12)

### Ошибки сборки
- Очистите `.next/` и `out/`
- Удалите `node_modules/` и выполните `npm install`
- Проверьте версию Node.js (требуется 18+)

### Проблемы с роутингом на хостинге
- Создайте файл `.htaccess` (см. [`BEGET_DEPLOYMENT.md`](./BEGET_DEPLOYMENT.md))

---

## 📞 Контакты

- **Репозиторий:** https://github.com/konkovev-cyber/avoska
- **Версия:** 0.1.5
